# Slack Configuration
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit your .env file to version control

# ===== Profile-Based Configuration (Recommended) =====
# If using profile-based configuration (config.json), set the tokens here:

# Default profile token
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Work profile token (if using multiple workspaces)
SLACK_WORK_BOT_TOKEN=xoxb-your-work-token-here

# Personal profile token (if using multiple workspaces)
SLACK_PERSONAL_BOT_TOKEN=xoxb-your-personal-token-here

# ===== Profile Selection =====
# Optional: Override the active profile (default: "default")
SLACK_AGENT_PROFILE=default

# ===== Direct Configuration (Legacy, no profiles) =====
# If not using profiles, these settings apply directly:

# Optional: Default channel for notifications (default: #general)
# Can be a channel (#channel) or direct message (@user)
SLACK_DEFAULT_CHANNEL=#general

# Optional: Request timeout in seconds (default: 30, range: 1-300)
SLACK_TIMEOUT=30

# Optional: Maximum retry attempts for failed requests (default: 3, range: 0-10)
SLACK_MAX_RETRIES=3

# ===== Application Behavior =====
# Optional: Path to custom config.json file
# SLACK_AGENT_CONFIG=/path/to/config.json

# Optional: Enable verbose (DEBUG) logging (0|1, default: 0)
SLACK_AGENT_VERBOSE=0

# Optional: Use JSON logging format (0|1, default: 0)
SLACK_AGENT_JSON_LOGS=0

# Optional: Disable credential masking in logs - ONLY for debugging (0|1, default: 0)
SLACK_AGENT_DEBUG=0

# Slack Agent Configuration (for slack_agent.py)
# Optional: Comma-separated list of channel IDs to monitor (default: auto-detect general channels)
# Example: C1234567890,C0987654321
SLACK_AGENT_CHANNELS=

# Optional: Polling interval in seconds (default: 5, minimum: 1)
SLACK_AGENT_POLL_INTERVAL=5